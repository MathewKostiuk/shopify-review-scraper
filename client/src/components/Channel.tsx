type ChannelProps = {
  id: number,
  name: string,
  url: string,
  brand_id: number,
};

export default function Channel(props: ChannelProps): JSX.Element {
  return (
    <div>
      Channel
    </div>
  )
}
